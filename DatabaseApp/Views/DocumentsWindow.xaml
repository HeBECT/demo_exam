<Window x:Class="DatabaseApp.Views.DocumentsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Документы" Height="600" Width="1000"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
            <Button Content="Добавить" Width="100" Height="30" Margin="5" 
                    Click="AddButton_Click"/>
            <Button Content="Удалить" Width="100" Height="30" Margin="5" 
                    Click="DeleteButton_Click"/>
            <Button Content="Обновить" Width="100" Height="30" Margin="5" 
                    Click="RefreshButton_Click"/>
        </StackPanel>

        <DataGrid Grid.Row="1" x:Name="DocumentsDataGrid" 
                  AutoGenerateColumns="False" 
                  IsReadOnly="True"
                  SelectionMode="Single"
                  Margin="10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding DocumentId}" Width="50"/>
                <DataGridTextColumn Header="Номер" Binding="{Binding DocumentNumber}" Width="120"/>
                <DataGridTextColumn Header="Дата" Binding="{Binding DocumentDate, StringFormat=dd.MM.yyyy}" Width="100"/>
                <DataGridTextColumn Header="Тип" Binding="{Binding DocumentType}" Width="120"/>
                <DataGridTextColumn Header="Сумма" Binding="{Binding TotalAmount, StringFormat=N2}" Width="120"/>
                <DataGridTextColumn Header="Описание" Binding="{Binding Description}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
